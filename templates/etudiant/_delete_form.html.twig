<form method="post" action="{{ path('etudiant_delete', {'cin': etudiant.cin}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant.cin) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
