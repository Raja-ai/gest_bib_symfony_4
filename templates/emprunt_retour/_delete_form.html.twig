<form method="post" action="{{ path('emprunt_retour_delete', {'idEmpruntRetour': emprunt_retour.idEmpruntRetour}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emprunt_retour.idEmpruntRetour) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
