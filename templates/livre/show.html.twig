{% extends 'base.html.twig' %}

{% block title %}Livre{% endblock %}

{% block body %}
    <h1>Livre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdLivre</th>
                <td>{{ livre.idLivre }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ livre.titre }}</td>
            </tr>
            <tr>
                <th>NbrePages</th>
                <td>{{ livre.nbrePages }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ livre.prix }}</td>
            </tr>
            <tr>
                <th>DateAchat</th>
                <td>{{ livre.dateAchat ? livre.dateAchat|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ livre.disponible ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Couverture</th>
                <td>{{ livre.couverture }}</td>
            </tr>
            <tr>
                <th>LienTelechargement</th>
                <td>{{ livre.lienTelechargement }}</td>
            </tr>
            <tr>
                <th>NbreTelechargement</th>
                <td>{{ livre.nbreTelechargement }}</td>
            </tr>
            <tr>
                <th>EtatLivre</th>
                <td>{{ livre.etatLivre }}</td>
            </tr>
        </tbody>
    </table>
 <a href="{{ path('livre_edit', {'idLivre': livre.idLivre}) }}" class="btn btn-success btn-sm">Modifier</a>

    {{ include('livre/_delete_form.html.twig') }}
    <a href="{{ path('livre_index') }}" class="btn btn-primary btn-sm">Retourne à la list</a>

   
{% endblock %}
