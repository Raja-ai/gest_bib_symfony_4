{% extends 'base.html.twig' %}

{% block title %}Edit Livre{% endblock %}

{% block body %}
    <h1>Modifier les données d'un livre</h1>

    {{ include('livre/_form.html.twig', {'button_label': 'Modifier'}) }}
 {{ include('livre/_delete_form.html.twig') }}
    <a href="{{ path('livre_index') }}" class="btn btn-primary btn-sm">Retourne à la list</a>

   
{% endblock %}
