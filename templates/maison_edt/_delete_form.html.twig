<form method="post" action="{{ path('maison_edt_delete', {'idMaisonEdt': maison_edt.idMaisonEdt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maison_edt.idMaisonEdt) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
