{% extends 'base.html.twig' %}

{% block title %}Edit Theme{% endblock %}

{% block body %}
    <h1>Modifier un theme</h1>
   
    {{ include('theme/_form.html.twig', {'button_label': 'Modifier'}) }}
    {{ include('theme/_delete_form.html.twig') }}

    <a href="{{ path('theme_index') }}" class="btn btn-primary btn-sm">Retourne à la list</a>

{% endblock %}
