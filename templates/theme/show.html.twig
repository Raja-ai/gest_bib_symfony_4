{% extends 'base.html.twig' %}

{% block title %}Theme{% endblock %}

{% block body %}
    <h1>Theme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTheme</th>
                <td>{{ theme.idTheme }}</td>
            </tr>
            <tr>
                <th>DesignTheme</th>
                <td>{{ theme.designTheme }}</td>
            </tr>
        </tbody>
    </table>
<a href="{{ path('theme_edit', {'idTheme': theme.idTheme}) }}" class="btn btn-success btn-sm">Modifier</a>

    {{ include('theme/_delete_form.html.twig') }}
    <a href="{{ path('theme_index') }}" class="btn btn-primary btn-sm">Retourne à la list</a>

    
{% endblock %}
